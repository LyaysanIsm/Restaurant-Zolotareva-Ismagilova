@model Dictionary<int, (string, int, int)>

@{
    ViewData["Title"] = "Холодильник";
}

@section Links {
    <div class="navbar-collapse collapse d-sm-inline-flex flex-sm-row-reverse">
        <ul class="navbar-nav flex-grow-1">
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="" asp-controller="Request" asp-action="Request">Заявки </a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="" asp-controller="Fridge" asp-action="ListFridges">Холодильники </a>
            </li>
        </ul>
    </div>
}
<div class="text-left">
    <h4>Холодильник №@ViewBag.FridgeId</h4>
</div>

<hr />
<div>
    <a class="text-dark" asp-action="ListFridges">Вернуться назад</a>
</div>
<hr />

<p><a class="btn btn-primary" asp-controller="Food" asp-action="ListFoods" asp-route-fridgeId="@ViewBag.FridgeId">Пополнить холодильник</a></p>

<table class="table table-hover table-bordered">
    <thead class="thead thead-dark">
        <tr>
            <th>
                Продукт
            </th>
            <th>
                Свободно
            </th>
            <th>
                Зарезервировано
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Value.Item1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Value.Item2)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Value.Item3)
                </td>
            </tr>
        }
    </tbody>
</table>